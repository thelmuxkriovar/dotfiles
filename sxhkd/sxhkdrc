super + m
	kitty -e mountable
super + shift + m
	ktty -e unmountable
super + Return
	kitty
super + c
	rofiCalculator
super + d
	rofi -modi "drun,ssh" -show drun
super + shift + d
	rofiWebsite
super + i
	kitty -e htop
super + shift + a
	kitty -e alsamixer
super + shift + grave
	unicodeMenu
@ctrl + shift + alt + 4
	takeScreenshot
ctrl + alt + l
	lockScreen
F6
	manOpen
ctrl + super + { Left, Down, Up, Right }
	moveCursor 'focus parent' 'focus { left, down, up, right }' 'focus child'
super + y
	mpv-playlist appender
ctrl + alt + { Left, Right }
	moveCursor 'workspace { prev, next }'
super + { Left, Down, Up, Right }
	moveCursor 'focus { left, down, up, right }'
super + { h, v }
	moveCursor 'split { h, v }'; notify-send 'tile { horizontally, vertically }'
super + f
	moveCursor 'fullscreen toggle'
super + { s, w }
	moveCursor 'layout { stacking, tabbed }'
super + e
	moveCursor 'layout toggle split'
ctrl + grave 
	startDiscord
super + { KP_Add, KP_Subtract }
	i3-msg "gaps inner all { plus, minus } 2"
ctrl + shift + KP_Enter
	i3-easyfocus -a

